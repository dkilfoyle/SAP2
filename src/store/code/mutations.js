export function updateSourceCode(state, codeText) {
  state.sourceCode = codeText
}
export function updateTextBits(state, bits) {
  state.textBits = bits
}
export function updateDataBits(state, bits) {
  state.dataBits = bits
}
